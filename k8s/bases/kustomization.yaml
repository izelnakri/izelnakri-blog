kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

bases:
  - nginx-ingress # NOTE: how to make ingress IP stable? chart doesnt allow namespace specifications on the arguments!
  - docker-registry
  - gitea-helm-chart
  # - prometheus # NOTE: FOCUS, it takes time to delete prometheus
  - izelnakri-com
  - drone # TODO: Relies on the secret key generated by gitea,

resources:
# - charts/nginx-ingress.yaml
# - charts/docker-registry.yaml
# - charts/gitea-helm-chart.yaml
# - charts/drone.yaml
# - charts/stolon.yaml
# - charts/prometheus.yaml


# patchesJson6902:
# - target:
#     namespace: cert-manager
#   path: charts/cert-manager.yaml
