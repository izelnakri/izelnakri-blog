For scaleway use these annotations: service.beta.kubernetes.io/scw-loadbalancer-proxy-protocol-v2, service.beta.kubernetes.io/scw-loadbalancer-use-hostname
Learn Vault more for User + Secret management + association

move DNS to digitalocean or smt

add CI email notifier

also make github + actions + deploy workflow
otherwise have to implement a k8s gitea + CI system

also implement distributed elixir cluster
iex --name izzy@127.0.0.1 --cookie alice -S mix
iex() Node.connect(:$name:$ip)
iex() Node.list

