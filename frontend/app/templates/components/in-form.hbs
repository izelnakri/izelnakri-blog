{{yield (hash
  isLoading=isLoading
  model=model
  showErrors=showErrors
  button=(component "in-button" form=this)
  input=(component 'inputs/generic-input' form=this)
  inputpassword=(component 'inputs/input-password' form=this)
  textarea=(component 'inputs/input-textarea' form=this)
  posteditor=(component 'inputs/post-editor' form=this)
)}}
